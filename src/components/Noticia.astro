---
const { title } = Astro.props;
const { image } = Astro.props;
---

<div
  class="bg-black w-[472px] p-4 rounded-md min-h-56 flex flex-col justify-end relative overflow-hidden"
  style={`background-image: url('${image}'); background-size: cover; background-position: center; transition: transform 0.3s ease-in-out;`}
>
  <div
    class="absolute inset-0 bg-gradient-to-b from-transparent to-black rounded-md"
  >
  </div>
  <h1 class="text-2xl font-bold text-white relative z-10">{title}</h1>
</div>

<style>
  .bg-black {
    transition: all 0.3s;
    cursor: pointer;
  }

  .bg-black:hover {
    transform: scale(1.1);
    border: 2px solid white;
  }
</style>
